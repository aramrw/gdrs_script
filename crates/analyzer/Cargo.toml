[package]
name = "analyzer"
version = "0.1.0"
edition = "2021"

[dependencies]
tower-lsp = "0.20"
tokio = { version = "1.0", features = ["full"] }
tree-sitter = "0.20"
tree-sitter-solar = { path = "../tree-sitter-solar" }
